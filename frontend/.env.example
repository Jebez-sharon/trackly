# Copy this to frontend/.env.local and fill in the values.
# Vite only exposes variables prefixed with VITE_ to the browser, so never
# put a secret in here - anything in this file ships to the client.

# Base URL of the Flask API. Leave blank in development: src/lib/api.js falls
# back to http://localhost:5000.
VITE_API_URL=